---
interface Props { caption?: string; credit?: string }
const { caption, credit } = Astro.props;
---
{caption || credit ? (
  <figcaption>
    {caption ? <span>{caption}</span> : null}
    {credit ? <span class="media-credit">{credit}</span> : null}
  </figcaption>
) : null}
