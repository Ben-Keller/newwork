# Verification report

This report covers the completed local implementation candidate. It is a verified internal prototype, not a rights or public-launch approval. The production adapter will not publish the supplied fixture material.

## Build under test

| Field | Value |
|---|---|
| Verification time | 2026-08-17T21:34:41Z (Atlantic/Reykjavik / UTC) |
| Repository | Local Git repository initialized on `main`; record the final release SHA after owner review |
| OS | macOS 26.4.1, Apple silicon |
| Node / pnpm | 22.22.1 / 11.19.0 |
| Content mode | `prototype` |
| Browser matrix | Chromium desktop/mobile, Firefox desktop, WebKit desktop/mobile, plus Codex in-app Chromium |

## Final observed automated results

| Status | Check | Observed result |
|---|---|---|
| **PASS** | `pnpm install` | Lockfile updated; 1,279 packages restored from the content-addressable store with two downloads; package policy check passed. |
| **PASS** | `pnpm typecheck` | 145 files; 0 errors, 0 warnings, 0 hints. |
| **PASS** | `pnpm lint` | Exit 0 with no ESLint diagnostics. |
| **PASS** | `pnpm test` | 9 files / 116 tests passed, including exact four-track response constants, frame-rate compensation, monotonic first-order convergence, supplied-brand normalization, three-profile About normalization, and public/preview selected-work query gates. |
| **PASS** | `pnpm test:coverage` | 9 files / 115 tests passed. Coverage: 77.57% statements, 65.62% branches, 90.90% functions, and 83.45% lines; every enforced floor passed. |
| **PASS** | `pnpm build` | 46 static pages built, including all 16 prototype project routes and the standalone gallery routes. |
| **PASS** | `pnpm verify:dist` | 23 HTML pages and 472 built files passed media existence, secret/internal-field scan, security-header, analytics-host, noindex, and sitemap checks. |
| **PASS** | Full Playwright regression sweep | The post-redesign five-browser sweep ran 235 checks: 209 passed, 24 expected capability/opt-in cases skipped, and two WebKit history-return assertions exposed one route-order race. That race was fixed, then the sensitive motion/return matrix passed 10/10 across desktop/mobile Chromium, Firefox, and desktop/mobile WebKit. The six visual baselines also passed in a clean no-update run. |
| **PASS** | Gallery first-order scroll regression | Ten focused layout/motion checks passed across desktop/mobile Chromium, Firefox, and desktop/mobile WebKit. All four desktop tracks read the same raw `window.scrollY` target and underlying document distance, then use frame-rate-compensated response coefficients `0.095`, `0.075`, `0.125`, and `0.110`. Both the visible offset and the filter’s internal backlog are bounded to 100px, preventing fast gestures from saturating every lane and releasing a synchronized secondary push after native scrolling stops. The suite verifies the 4/2/2 responsive geometry, separation at gesture start and during a 2,000px fast jump, continued settling after a sudden stop, reversal response, near-synchrony during steady movement, monotonic convergence to zero, no CSS scroll-transform transition, and no competing smooth-scroll system. Tablet uses the first two response profiles; the two mobile lanes remain untransformed. `/?motionDebug=1` reports target, filtered value, lag, and coefficient per active track. |
| **PASS** | Gallery entrance zoom regression | The gallery now opens at 112% scale, holds for 450ms, then eases to its exact resting geometry over 1.8 seconds on an isolated stage, so horizontal pointer follow and weighted column transforms remain independent. Five focused browser profiles passed the entrance timing/scale/overflow contract; an eight-check desktop suite passed packing, pointer, reduced-motion, Save-Data, no-JavaScript, and browser-Back restoration. Masonry measures untransformed layout height, preventing the temporary zoom from becoming permanent lane gaps. All six reduced-motion visual baselines passed without updates. |
| **PASS** | Gallery preview stability regression | Preview eligibility is now refreshed from vertical viewport exposure on scroll/resize rather than from transformed video-node intersections, with separate 70% enter and 35% exit thresholds. A production-bundle pointer stress test produced zero source, poster, playback-state, or fallback mutations while preserving the same two active desktop previews. Nineteen focused checks passed across Chromium, Firefox, and WebKit desktop/mobile with five expected capability skips; the one development-server reload interruption passed immediately on rerun. Touch retains one active preview, and reduced-motion/Save-Data remain static. |
| **PASS** | Image-only gallery regression | Twenty focused checks passed across desktop/mobile Chromium, Firefox, and WebKit. Every cover is placed directly at the card size with zero inset padding, bounding border, poster outline, or angled crop. Cards have no persistent title/client/type/index rows or separator rules; each link retains its accessible project name, hover/focus reveals one title only, no-JavaScript remains usable, and packed lane gaps stay within the tightened bounds before and after weighted scrolling. The refreshed desktop/mobile Home baselines passed cleanly without update mode after visual inspection. |
| **PASS** | Open-ended portfolio labeling | Project-position UI, project totals, index-labeled reading progress, experimental-template ordinals, and all About work/people numbers are absent. About supporting media uses direct title/client captions without “Selected work” labels. Previous/next links remain browsing conveniences without presenting the fixture set as a ranked or complete sequence. |
| **PASS** | About mosaic regression | The About source order is Michael then Oliver, matching the footer People directory. Both profiles render five-tile rectangular mosaics from explicitly curated, owner-matched work selections. Narrow white gutters separate every orthogonal tile, and no gallery item uses an angled, polygonal, or notched crop. Hover and keyboard focus retain the caption and contained image emphasis without changing the gallery footprint. Focused JavaScript/no-JavaScript ordering checks and clean desktop/mobile visual baselines passed. Reduced motion removes image scaling. |
| **PASS** | Opening-title lifecycle regression | The complete five-second media-mask, independent flip, and solid-black settle sequence passed in all five browser profiles. The single-line supplied title spans the available width without the former deliberate top/bottom crop. The static, accessible `film + photo production agency` descriptor sits immediately below it in small sentence-case type. Four-width geometry, descriptor placement, and aligned per-letter hover targeting passed on desktop and mobile Chromium. Interrupted first-session runs also passed in all five profiles: route disposal cancels the active composition without storing completion, and the next Home visit retries rather than flashing to the end. |
| **PASS** | Supplied identity integration | The owner-supplied `new-black.svg` remains unchanged across canonical source, web-ready, and served brand copies. Header and footer show the NW mark without an adjacent `Agency` qualifier and link conventionally to Home while retaining the accessible site name. Dark project canvases force the persisted header and footer artwork to white, including after client-side navigation. Focused desktop/mobile Chromium checks passed, footer overflow remained zero, and the favicon remains a deliberate rounded-white-square derivative. |
| **PASS** | Supplied home-title letter kits | The uploaded archive is checksum-identical to the active owner-supplied kit at `assets/source/brand/new-work-title-letter-kit`; its exact `0 0 5176.81 834` master and seven standalone SVG masks preserve the stable selector contract and now render at 92% of the available inline measure. The sentence-clean and original legacy kits remain intact, served, and selectable through the same `ACTIVE_TITLE_KIT` switch. `LogoIntro.astro` derives stage aspect ratio, inline markup, mask file, public root, and session key per kit. Focused checks cover the full five-second opening, seven hover treatments, 1440/1024/390/320 single-line geometry, active public asset response, and non-blocking header/gallery access. |
| **PASS** | Michael portfolio expansion | Audited Michael's public portfolio against the existing library, excluded duplicate project imagery and site/logo material, and retained 24 distinct source renditions plus 24 normalized WebP review copies. Prototype Home currently selects 15 standalone Michael tiles alongside 14 routed-project cards; all 16 project routes remain built, and production excludes the standalone set. The 29 visible tiles preserve the saved ordering behavior without deleting source content. Six focused desktop/mobile JavaScript and no-JavaScript gallery checks passed with zero horizontal overflow. Canonical and app-local manifests retain source URL, dimensions, checksum, derivation, and prototype/master-review blockers. |
| **PASS** | Editorial footer regression | Fifteen focused checks passed across desktop/mobile Chromium, Firefox, and WebKit for the four-line studio statement, CMS-backed People directory, route-aware Explore links, contact/social destinations, oversized supplied wordmark, legal baseline, Contact-page email deduplication, full-height desktop/mobile geometry, and horizontal-overflow bounds. All six Home/About/project visual baselines were refreshed, inspected, and then passed cleanly without update mode; the full 20-check Axe matrix also passed. |
| **PASS** | Gallery tooltip removal | Gallery cursor tooltips and their route-return animation were removed. Project names remain available programmatically and appear in the contained hover/focus overlay; the bounded media pan remains active for fine pointers. |
| **PASS** | Media-control icon regression | Four focused desktop/mobile browser checks passed. Play and pause render as state-specific SVG glyphs with no visible text, while the button continues to expose the correct changing accessible name and retain its 44px target. |
| **PASS** | Typography integration | Chromium loaded the local Futura/Futura Condensed display faces and PP Neue Montreal Book/Bold body faces through the repository aliases. Fifteen focused checks passed across five browser profiles for the single-line home title, two-person About composition, and 320/390/1024/1440 overflow geometry. No commercial desktop font file was copied into the repository. |
| **PASS** | Accessibility automation | 20 of 20 focused Axe checks passed across the verified routes and viewport profiles. |
| **PASS** | Visual regression | Six of six desktop/mobile Chromium baselines passed in clean no-update mode after the print-editorial redesign, including full-page About, Home, and project-detail views. |
| **PASS** | QA capture set | `scripts/capture-qa.mjs` refreshes 27 named states in `artifacts/qa/`, including the opening title's media, flip, and final-black states; About top plus Oliver and Michael compositions at desktop, tablet, 390px, and 320px; and the home, hover, transition, project, Contact, and 404 states. The cold-preview title-stage allowance is five seconds so the capture remains reliable without changing the page animation. |
| **PASS** | Built preview inspection | The generated static site passed the required gallery, About, Contact, still/motion project, related-project, explicit 404, and unknown-route checks at desktop and mobile widths. |
| **PASS** | Motion payload split | Static/reduced-motion entry JavaScript is approximately 8.7 KB gzip, down from approximately 68.0 KB gzip (about 59.3 KB saved). The heavier GSAP vendor and route runtime load only when an active motion hook and motion preference require them. |
| **PASS** | Project Studio integration | Registry validation completed with 0 errors and 0 warnings; the `new-work-portfolio` card, documentation, launch command, and single tracked port 4184 remain aligned. Desktop/mobile card art and all three detail screenshots were refreshed, with the third showing the complete Michael/Oliver About sequence. The Project Studio server was not running during this final asset refresh; its prior registered URL check returned HTTP 200. |
| **PRIOR PASS** | Local prototype Lighthouse | The latest stored three-run mobile-simulated matrix measured Home performance at 97, the earlier sparse About page at 99, and the sampled project at 98–99; accessibility was 100 throughout. That run predates the expanded About editorial content, so its About score is not claimed as a fresh result. The new profile media remains below the text-led opening and lazy-loaded; CI budgets remain configured for the next deployed/served audit. |
| **PASS** | GROQ safety execution | Real `groq-js` evaluation accepts only fully approved synthetic projects/Notes and rejects rights, review, placeholder, incomplete-media, and hidden variants. |
| **PASS** | Seed behavior tests | Preservation/force-update, sticky blockers, asset replacement, idempotence, option parsing, and dry-run planning are covered locally without external writes. |

## Live browser inspection

The redesigned local application was inspected in the in-app browser at desktop and narrow mobile widths, with the repeatable 27-state capture set used as the visual handoff.

- The visual system now uses a warmer printed-paper canvas, more visible archival grain, heavier black rules and display hierarchy, compact numbered captions, and strictly monochrome action treatments. Section markers, project action overlays, contact strips, progress, capability indexes, and All Work navigation use black surfaces with paper-white type; no rounded cards, shadows, gradients, or borrowed reference artwork were introduced.
- Home rendered 14 routed-project cards plus 15 prototype-only Michael portfolio images—29 tiles total—while all 16 project routes remained available in the build. The page retains `noindex, nofollow`, a clipped 4/2/2 editorial contact sheet, and no document-level horizontal overflow. The Michael tiles remain unnamed source-library media and disappear entirely in production mode. The visible “Selected projects” rail and every permanent below-card name, client, type, index, and rule remain absent. Every regular card fills its lane at one consistent responsive width; 4–8px horizontal gutters, an 8px packed rhythm, and a 12px maximum art-directed offset produce the near-tessellated composition. The four desktop tracks retain unequal top and bottom edges; the outer tracks bleed past and are clipped by the gallery viewport. Muted preview videos start automatically once 70% visible without waiting for hover or focus, pause and detach offscreen, and remain bounded to two desktop or one mobile stream.
- The opening is now a generous in-flow, single-line title stage: up to eight real project posters and one muted preview play through the large block `NEW WORK` mask, selected cells flip independently, and the media layer fades at five seconds to retain the identical name in solid black. It runs once per session without blocking the header or work; returning, reduced-motion, Save-Data, and no-JavaScript visits receive the black resting state immediately.
- The manifesto now begins directly with its rule and sentence-case copy, with the former “Statement / NW 001” metadata strip removed. Its broad editorial measure is approximately 977px and three rendered lines at 1440px, with a natural four-line mobile wrap. The count of visible characters maps directly to scroll progress and reverses when scrolling upward; reduced-motion and no-JavaScript paths render the complete statement immediately.
- Hover and keyboard-focus review covered the slower 720ms media scale, title-only overlays, removal controls, directional underlines, and route transitions. Gallery cursor tooltips are absent. Every project and standalone Michael tile remains programmatically named when visible metadata is absent; coarse-pointer and reduced-motion states omit nonessential pointer animation.
- The cinematic, photo-essay, campaign, and experimental project layouts were visually checked as genuinely distinct templates, including transition destinations, poster retention, and media controls.
- All 16 project routes derive one eager opening hero from the same gallery cover. Desktop composes title/description/metadata on the left with natural-ratio still or controlled motion on the right; mobile stacks copy, hero, then body. Cover-equivalent legacy blocks are removed below, while distinct films and still grids remain lazy and ordered. Mercury's five-film fixture group uses copy → supporting stills → full player → closing copy, preventing the blocked player poster from immediately repeating the hero.
- Sparse project bodies render two gated Lorem Ipsum text notes plus neutral geometric studies only in non-production. Long-title, 1024px masked-video control hit testing, 390px/320px wrapping, hero failure states, and horizontal overflow were inspected after fixes.
- About now opens directly with a large editorial statement, without the former “Information / 01 / 03” utility strip, then presents semantic Michael and Oliver profile chapters in the same order as the footer People directory. Both profiles use five intentionally curated owner-matched links in distinct rectangular mosaics. Fine white gutters separate the tiles without diagonal masking. Each source record remains locally packaged and independently blocked from production. The source order remains copy before media, mobile stacks naturally, project imagery is explicitly presented as work rather than portraiture, and empty provisional alt text is paired with linked project identity revealed on hover/focus. Contact likewise begins directly with its content, with no Information/fraction strip.
- About, Contact, Manifesto, project descriptions, roles, credits, and What-we-did disclosures render with unmistakable lorem ipsum review copy, including the placeholder `hello@lorem.ipsum` contact address.
- Mobile review at 390px and 320px confirmed a compact two-column home with no forced viewport-height card tails, readable project typography, one fully released preview source at a time, no custom cursor, and no horizontal overflow.

## Verified implementation coverage

- Three fail-closed content modes: fixture prototype, token-protected draft preview, and approved published production; preview/production never fall back to fixtures.
- Independent GROQ and adapter enforcement for visibility, schedule, review/safety flags, complete media, strict Sanity-hosted files, exact Vimeo/YouTube identity, rights evidence, and rights expiry.
- Runtime CMS payload validation before normalization; no untyped `any` escape hatches in application/schema/scripts.
- Project- and asset-level rights evidence, dates, expiry, territories/channels, provenance, and Studio readiness lists.
- Dry-run seed planning with no uploads/writes, checksum reuse, preservation-first updates, explicit force mode, and separate read/write token authority.
- All required media block renderers, authoritative Sanity crop/hotspot handling, responsive local/Sanity image sources, captions language/label, transcript fallback, icon-based play/pause controls with changing accessible names, embed recovery, and poster-first privacy.
- Reduced-motion and Save-Data paths, pause/user override, first-visual eager loading, later lazy loading, and bounded preview concurrency.
- Site-default and per-page SEO, explicit share-image alt, focal 1200×630 Sanity social crops, structured data, production-only canonical sitemap, nonproduction noindex, and 404 noindex.
- Presentation metadata for card size/column/offset/treatment, project theme/accent, title/hero treatment, layout variant, and motion intensity is modeled in fixtures, TypeScript, GROQ, normalization, and Sanity Studio.
- About profile names, owner mapping, role labels, biographies, supporting portfolio media, and safety flags are modeled in mirrored fixtures, TypeScript, GROQ, normalization, seed conversion, and Sanity Studio. Production filters provisional people individually, derives supporting media only from approved owner-matched projects, and independently excludes blocked explicit portfolio records.
- Responsive 4/2/2 equal-width work grid with dense lane backfill, clipped desktop outer tracks, bounded card offsets, automated lane-width and inter-card-gap checks, four first-order filters over one native scroll target, no per-lane distance multipliers or timing delays, no mobile column transform, polished hover/focus micro-interactions, safe-area gutters, long-value wrapping, gallery boundary checks, mobile/film overflow coverage, browser Back restoration, shared route transitions, and complete provisional editorial content surfaces without review-marker chrome.
- Four routed project systems—cinematic, photo essay, campaign, and experimental—share a gallery-derived copy-left/media-right hero and accessible media primitives while retaining distinct lower-body hierarchy, rhythm, typography, color, and motion behavior.
- The centralized motion lifecycle lazily loads GSAP, ScrollTrigger, Flip, SplitText, and CustomEase only for eligible routes, then cancels tweens, listeners, animation frames, and media work on route swaps.
- Skip link, keyboard/focus-managed menu, semantic captions/credits, media status announcements, Axe serious/critical checks, and Chromium/Firefox/WebKit automation.
- Conditional analytics/CSP hosts, output leak scanning, versioned security/cache headers, lockfile/dependency automation, CI quality/browser/Lighthouse jobs, Cloudflare Pages configuration, visual baselines, and release templates.

## Owner-controlled or manual release gates

| Status | Gate | Required next action |
|---|---|---|
| **BLOCKED** | Production Sanity verification | Supply the owner-controlled project/dataset and approved published records; run protected preview and production builds. |
| **BLOCKED** | Seed dry run and idempotent import against Sanity | Supply the selected project/dataset and narrowly scoped token; review dry-run output before any write. |
| **BLOCKED** | Rights/editorial approval | Complete every project and asset decision in `docs/CONTENT_READINESS.md`; current fixtures remain provisional. |
| **BLOCKED** | Final content and remaining identity evidence | Supply licensed font evidence, About/contact/SEO copy, social image/alt, and optional module decisions. The owner-supplied wordmark and favicon are now integrated. |
| **BLOCKED** | Cloudflare/Git hosting | Owner must create/connect accounts, protect the release branch, configure secrets/access/DNS/CORS/webhook, and deploy the verified commit. |
| **NOT RUN** | Production Lighthouse/RUM | CI budgets are configured; measure the approved deployed production-content origin and enable monitoring only after privacy approval. |
| **NOT RUN** | Manual assistive technology | Complete VoiceOver + Safari and NVDA + Firefox/Chrome using `docs/ACCESSIBILITY_QA.md`. |
| **NOT RUN** | Real device/forced-colors | Complete real Safari/iPhone portrait/landscape/safe-area, 200% zoom, text zoom, and forced-colors review. |
| **NOT RUN** | Backup/rollback drill | Owner must export the selected Sanity dataset and exercise restore/Cloudflare rollback in authorized accounts. |

## Disposition

**Local implementation behavior, automated QA, visual regression, and prototype Lighthouse budgets: PASS.**

**Public launch: BLOCKED pending owner content, rights, credentials, account configuration, deployment, production performance verification, and manual release checks.** No Sanity mutation, external deployment, DNS/account change, monitoring enablement, or rights decision was performed.
