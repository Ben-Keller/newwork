{
  "name": "new-work",
  "private": true,
  "version": "1.0.0",
  "main": "package.json",
  "license": "UNLICENSED",
  "scripts": {
    "build": "sanity build",
    "content:snapshot": "node scripts/export-production-snapshot.mjs",
    "content:validate": "sanity documents validate --dataset production --yes",
    "content:validate:ci": "sanity documents validate --dataset production --yes --level error",
    "lint": "eslint .",
    "migrate:about-page": "sanity exec scripts/migrate-about-page.ts --with-user-token --",
    "migrate:flat-assets": "sanity exec scripts/migrate-flat-assets.ts --with-user-token --",
    "migrate:project-ids": "sanity exec scripts/migrate-private-project-ids.ts --with-user-token --",
    "migrate:work-type": "sanity exec scripts/migrate-projects-to-work.ts --with-user-token --",
    "migrate:legacy-settings": "sanity exec scripts/remove-legacy-site-settings.ts --with-user-token --",
    "seed:with-user-token": "sanity exec scripts/seed-with-user-token.ts --with-user-token",
    "deploy": "npm run deploy:check && sanity deploy --no-build --schema-required",
    "deploy:check": "npm run schema:validate && npm run typegen && npm run typecheck && npm run lint && npm run build",
    "deploy:ci": "sanity deploy --no-build --schema-required --yes",
    "deploy-graphql": "sanity graphql deploy",
    "dev": "sanity dev",
    "schema:deploy": "sanity schemas deploy",
    "schema:validate": "sanity schemas validate --level error",
    "typegen": "sanity schemas extract --force --enforce-required-fields && sanity typegen generate",
    "start": "sanity start",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "sanity"
  ],
  "dependencies": {
    "@sanity/icons": "^5.2.1",
    "@sanity/ui": "^4.0.6",
    "groq": "^6.10.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "sanity": "^6.11.0",
    "styled-components": "^6.1.18"
  },
  "devDependencies": {
    "@sanity/eslint-config-studio": "^7",
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.14",
    "eslint": "^10.8.1",
    "prettier": "^3.5",
    "typescript": "^5.8"
  },
  "overrides": {
    "js-yaml": "3.15.1",
    "smol-toml": "1.6.1",
    "undici": "7.29.0",
    "uuid": "11.1.1"
  },
  "prettier": {
    "bracketSpacing": false,
    "printWidth": 100,
    "semi": false,
    "singleQuote": true
  }
}
